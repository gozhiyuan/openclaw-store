id: default-managed
version: 1
name: Default Managed Project
description: Minimal managed OpenClaw project that keeps the normal default
  workflow available while adding a single generalist entry-point team for
  structured installs.
source_usecase: Default Managed Project
entry_team: autonomous-startup
packs:
  - autonomous-startup
project_skills:
  - malaclaw-manager
installable_skills: []

tags:
  - default
  - managed
  - general
  - bootstrap
required_apis: []
required_capabilities: []
external_requirements: []
bootstrap_prompt: Start the Default Managed Project demo project from the
  malaclaw starter `default-managed`. Minimal managed OpenClaw project
  that keeps the normal default workflow available while adding a single
  generalist entry-point team for structured installs. Use `autonomous-startup`
  as the initial entry team. Review STARTER.md, confirm any missing external
  integrations, then break the work into clear team tasks.
